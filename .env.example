# AWS Configuration
AWS_REGION=ap-northeast-1

# AWS Cognito Configuration (Required for JWT authentication)
# These values will be output by terraform after applying the infrastructure
COGNITO_USER_POOL_ID=your-user-pool-id
COGNITO_CLIENT_ID=your-cognito-client-id

# Application Configuration
ENVIRONMENT=development

# Development Configuration
# For local development, you can use these values
# In production, these should be set through ECS task definition or secrets manager
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://thirdlf03.com